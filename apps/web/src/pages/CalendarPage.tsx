export function CalendarPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold">Content calendar</h1>
      <p className="mt-1 text-sm text-slate-500">
        AI-drafted plan; requires approval (Gate 2) before batch package generation.
      </p>
      {/* Phase 3: slot grid with drag-reorder, objective/pillar/format per slot, approve action */}
      <div className="mt-6 rounded-xl border border-dashed border-slate-300 p-12 text-center text-slate-400">
        Calendar UI — implementation plan Phase 3
      </div>
    </div>
  );
}
