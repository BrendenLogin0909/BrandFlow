export function PostPackagesPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold">Post packages</h1>
      <p className="mt-1 text-sm text-slate-500">
        Full package editor: hooks, variants, CTA, hashtags, first comment, per-field locking and
        part-level regeneration; linked visual package opens in the design editor.
      </p>
      {/* Phase 3: package list filtered by workflow status + package detail editor */}
      <div className="mt-6 rounded-xl border border-dashed border-slate-300 p-12 text-center text-slate-400">
        Post package editor — implementation plan Phase 3
      </div>
    </div>
  );
}
